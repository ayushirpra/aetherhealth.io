# Client Environment Variables — AetherHealth
# Copy to .env and fill in real values. Never commit .env.

# Backend API base URL (used when NOT using Vite proxy)
VITE_API_URL=http://localhost:5000/api
