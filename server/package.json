{
  "name": "aetherhealth-server",
  "version": "0.1.0",
  "description": "AetherHealth — Node.js/Express backend",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --experimental-vm-modules node_modules/.bin/vitest run"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "helmet": "^8.1.0",
    "express-rate-limit": "^7.5.1",
    "express-validator": "^7.2.1",
    "mongoose": "^8.14.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "supertest": "^7.1.0"
  }
}
